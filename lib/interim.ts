// ==========================================================================
//  🔴임시 스위치 — 정기결제가 늦어지는 동안만 켜 두는 것 (2026-08-21 사용자 결정)
//
//  국내·해외 정기결제가 **동시에** 열릴 때까지 MassLabs와 archiMap을 따로 굴린다:
//    · MassLabs = LaserFish 건당결제만 판다. 구독표·[내 구독]·구독 결제 화면을 감춘다.
//    · archiMap = 로그인만 하면 PLUS 무료로 열어 준다.
//
//  🔴2026-09-02 방향 전환 — 국내 PG로는 **해외카드 정기결제가 어디서도 안 된다**는
//    답을 갤럭시아·토스페이먼츠·엑심베이 세 곳에서 받았다. 그래서 국내 PG를 하나씩
//    붙여 가는 길을 접고, 국내·해외를 한 번에 덮는 **MoR**(Paddle·Creem 등 — 그쪽이
//    법적 판매자가 되어 결제와 각국 부가세를 대신 진다)로 간다.
//    · 하루 붙였던 토스페이먼츠 정기결제는 **심사 중 해지**했다(33만원 환급).
//      한국 회원이 쌓이면 국내 전용으로 다시 붙인다 — 배선은 지우지 않았다.
//    · 엑심베이 토큰빌링도 더 기다리지 않는다.
//  ⛔그래서 이 스위치는 **그대로 false**다. MoR 심사가 통과하고 결제가 실제로 도는 것을
//    확인한 날 켠다. 그전에 켜면 손님이 열리지 않는 결제창 앞에 선다.
//
//  🔴되돌리는 법 = 아래 값을 true로 바꾸는 것 하나뿐이다.
//    구독 코드는 **하나도 지우지 않았다** — /account · /subscribe · PlanTable ·
//    /api/subscribe/* · lib/subscription.ts 는 그대로 있고 flag 뒤에 숨어 있을 뿐이다.
//  ⚠️archiMap 스위치는 저장소가 달라 같이 못 뒤집는다 — 되돌릴 때
//    `D:\CODE\archiMap public/app.js` 의 SOLO_PLUS_FREE 도 함께 false로 내릴 것.
// ==========================================================================
export const SUBSCRIPTION_LIVE: boolean = false;

// ==========================================================================
//  🔴홈에서 건당결제를 감춘다 (2026-08-29 사용자 결정)
//
//  홈(components/LandingView) 5구역에 구독표와 LaserFish 건당표가 나란히 서
//  있었는데, 건당결제 안내의 정본은 **LaserFish 소개 사이트**다
//  (설치 안내를 그쪽으로 넘긴 2026-08-28 결정과 같은 결이다).
//  ⇒ 홈에서는 오른쪽 건당 칸만 감추고 구독 칸만 남긴다. 값과 그림은
//    그대로 살아 있다 — 이 값을 true로 되돌리면 다시 선다.
//
//  ⚠️이건 **홈에만** 걸리는 스위치다. `/price` 는 지금도 건당표를 그린다 —
//    SUBSCRIPTION_LIVE 가 false 인 동안 그 화면에 남는 내용물이 그것뿐이다.
//    밖에서 들어오는 문을 2026-08-29 에 다 훑었고, 셋 다 지금은 닫혀 있다:
//      · ~~배포된 라이노 플러그인~~ — 아니다. `/plan` 을 무는
//        PLAN_URL·Gate 는 LaserCuttingDrawings 의 **커밋 안 된 작업본**에만 있고,
//        릴리스된 2.2.3 은 `/payment`·`/api/verify-payment` 뿐인 건당결제다.
//        그 작업본을 배포하는 날 이 길이 살아난다.
//      · ~~archiMap 의 [구독 해지]~~ — 2026-08-29 `/account` 로 돌렸다(사용자 결정:
//        해지는 /account 한 곳에서만). 그쪽 public/app.js 의 MANAGE_URL.
//      · ~~LaserFish 소개 사이트의 [자세히 보기]~~ — 2026-08-29 껐다
//        (그쪽 lib/site.ts 의 BUY_LIVE). 켜지는 날 나가는 곳은 /price 가 아니라
//        홈의 가격 구역(`/#pricing`)이다.
//    ⇒ 지금 /price 로 들어오는 길은 MassLabs 자기 메뉴([비용])뿐이다. 그래서
//      /price 의 건당표를 옮기는 일은 급하지 않다 — SUBSCRIPTION_LIVE 를 켜는 날
//      (구독표가 그 자리를 채우는 날) 함께 볼 것.
// ==========================================================================
export const PER_PIECE_ON_HOME: boolean = false;

// ==========================================================================
//  🔴"값을 보러 가는 곳" — 화면들이 [비용]·[구독하기]로 내보내는 주소.
//
//  2026-08-29 사용자 결정으로 **/price 를 홈의 가격 구역으로 접었다.**
//  구독표가 거기 한 벌뿐인데 /price 는 건당표를 그리고 있어서, [Subscribe] 를
//  누른 사람이 조각당 단가표 앞에 서는 일이 벌어졌다.
//    · 구독을 안 파는 동안(SUBSCRIPTION_LIVE=false) → 홈 가격 구역 `/#pricing`
//      (next.config.ts 가 /price 주소 자체도 그리로 넘긴다 — 307)
//    · 구독을 파는 날 → 다시 `/price`. 그 화면은 지우지 않았다.
//  🔴메뉴·단추는 이 값 하나만 본다. 화면 파일에 주소를 다시 적지 말 것 —
//    한쪽만 고쳐지면 스위치를 뒤집는 날 죽은 링크가 남는다.
//  ⚠️archiMap·LaserFish 도 같은 곳(`/#pricing`)을 가리킨다(2026-08-29). 저쪽은
//    저장소가 달라 이 값을 못 읽는다 — 되돌리는 날 손으로 함께 볼 것.
// ==========================================================================
export const PRICING_HREF: string = SUBSCRIPTION_LIVE ? "/price" : "/#pricing";

// ==========================================================================
//  🔴테스트 채널 스위치 — 실결제 없이 정기결제 흐름만 볼 때 켠다.
//
//  포트원 콘솔의 **테스트 연동** 채널이다. 테스트 카드번호로 빌링키 발급 →
//  confirm → 구독 반영까지 전 구간을 돈 없이 확인할 수 있다.
//
//  ⚠️테스트 채널은 결제모듈을 전부 열어 주므로 **"실제로도 된다"는 증거가
//    아니다.** 실계약에서 정기결제가 열려 있는지는 PG가 답해야 알 수 있다
//    (엑심베이는 "개인사업자 정기결제 입점 불가"라는 안내를 받아 확인 중).
//  🔴시험이 끝나면 반드시 false로 되돌릴 것 — 켠 채로 배포하면 손님 결제가
//    테스트 채널로 나가 돈이 안 들어온다.
// ==========================================================================
export const USE_TEST_CHANNELS: boolean = false;

export const TEST_CHANNEL_KRW = "channel-key-b5054294-344b-4833-8f5a-7f3a445d4b40";
export const TEST_CHANNEL_INTL = "channel-key-6e915a7e-6083-4af1-a301-6eeb7fa4ce72";

// ==========================================================================
//  🔴할인 기간 — 로그인하면 PLUS (2026-09-05 사용자 결정)
//
//  archiMap 은 2026-08-21 부터 `SOLO_PLUS_FREE`(그쪽 public/app.js)로 이미 이렇게
//  굴러왔다: 로그인한 사람은 전부 PLUS 로 본다. 그런데 LaserFish 는 건당결제로
//  따로 팔고 있어서 그 규칙 밖에 있었다.
//  ⇒ 건당결제를 폐기하고 LaserFish 문턱을 PLUS 로 내리면서(lib/plans 의 MIN_PLAN),
//    두 프로그램이 같은 규칙 아래 선다 — **로그인하면 PLUS, 당분간 공짜.**
//
//  🔴이 값은 SUBSCRIPTION_LIVE 하나에서 파생된다. 스위치를 둘로 두면 언젠가
//    "구독은 파는데 여전히 공짜"인 상태가 생긴다 — 그건 돈이 안 들어오는 상태다.
//
//  🔴판정을 얹는 곳은 **아래 effectivePlan 한 함수뿐이다.** 서버(lib/plugin-auth 의
//    entitlementOf)도, DB 값을 직접 읽는 화면들(/account · /price · 홈의
//    lib/use-my-plan)도 전부 그것을 부른다 — 그래야 라이노 플러그인·요금제 표·
//    내 계정이 같은 답을 본다. ⛔화면마다 "지금은 무료" 판정을 새로 적지 말 것.
//    ⚠️2026-09-05 이전에는 이 규칙이 entitlementOf 안에만 있었다. 화면들이 my_plan
//      RPC 를 날로 읽으면서 같은 사람이 서버에선 PLUS, /account 에선 미구독이었다.
//  ⚠️DB(profiles.plan · subscriptions)는 **안 건드린다.** 진짜 등급은 free 그대로다 —
//    거기에 plus 를 적으면 행사가 끝난 뒤에도 전원이 PLUS 로 남는다
//    (archiMap 이 같은 이유로 ACCT.plan 을 안 건드린다).
//
//  ⚠️화면에 "(할인 기간)" 이라고 적는 자리가 셋이다. 이 값이 false 가 되면 셋이
//    함께 사라진다 — components/PlanTable · components/LandingView, 그리고
//    저장소가 달라 손으로 봐야 하는 archiMap 의 PLAN 창(그쪽 renderSubscription).
// ==========================================================================
export const PLUS_FREE_PROMO: boolean = !SUBSCRIPTION_LIVE;

/**
 * 진짜 등급(DB)을 **화면·플러그인에 답할 등급**으로 바꾼다.
 *
 * 🔴할인 기간에는 로그인만 하면 PLUS 다. 그 한 줄이 여기 한 곳에만 있다 —
 *   전에는 서버(lib/plugin-auth 의 entitlementOf)에만 있었는데, 화면들이
 *   DB 값을 직접 읽으면서 같은 사람이 홈에서는 PLUS, /account 에서는 free 로
 *   보이는 일이 생겼다(2026-09-05 발견).
 * ⛔올리는 것은 free 뿐이다. 돈을 낸 pro·max 를 plus 로 끌어내리면 안 된다.
 * ⚠️DB 에는 안 적는다 — 답할 때만 올려 준다. 적어 버리면 행사가 끝난 뒤에도
 *   전원이 PLUS 로 남는다.
 */
export function effectivePlan(real: string | null | undefined): string {
  const r = real || "free";
  return PLUS_FREE_PROMO && r === "free" ? "plus" : r;
}

// ==========================================================================
//  ⛔건당결제 폐기 (2026-09-05 사용자 결정)
//
//  LaserFish 를 조각당 $0.1/$0.05 로 팔던 배선이다. 구독(PLUS)이 LaserFish 를
//  덮게 되면서(lib/plans 의 MIN_PLAN) 같은 것을 두 번 파는 꼴이 되어 접었다.
//
//  🔴이 값이 false 인 동안 `/payment` 는 결제창을 그리지 않고 **안내문**을 띄운다:
//    "건당결제는 끝났습니다. 플러그인을 새로 받으면 로그인만으로 열립니다."
//    ⚠️화면을 통째로 없애지 않는 이유 — 배포된 옛 플러그인(2.2.3)이 이 주소를
//      직접 연다. 404 를 띄우면 사람이 무슨 일이 난 건지 알 길이 없다.
//
//  🔴`/api/verify-payment` 는 **살려 둔다.** 이 스위치를 내리는 순간에 결제창을
//    이미 띄워 둔 사람이 있을 수 있고, 그 사람의 폴링이 영영 성공하지 않으면
//    돈만 내고 도면을 못 받는다. 새로 시작하는 길만 막고, 이미 시작된 것은 끝나게 둔다.
//
//  🔴함께 죽은 것 — `/api/submit-review`(paymentId 로 신원을 삼던 후기 저장)와
//    결제 완료 뒤 후기 화면으로 자동으로 넘기던 배선. 후기는 이제 `/api/reviews` 다.
//
//  ⚠️되살릴 일은 없다고 보지만, 값·그림·화면은 하나도 안 지웠다
//    (components/PerPiecePricing · app/payment · PER_PIECE_ON_HOME).
// ==========================================================================
export const PER_PIECE_LIVE: boolean = false;
